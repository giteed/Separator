# requirements.txt
# python>=3.11

# Убедитесь, что pip обновлён до последней версии
# pip install --upgrade pip

# prompt_toolkit - Это библиотека Python для создания интерактивных командных интерфейсов с поддержкой подсветки синтаксиса, автозавершения и многострочного ввода.
prompt_toolkit

# rich - Красивое форматирование вывода в терминале (таблицы, прогресс-бары, текст с подсветкой)
rich

# click - Создание удобных интерфейсов командной строки
click

# rich-click - Для форматирования справки и вывода команд с цветным интерфейсом
rich-click
